# Event Level \(Backward Compatible Views\)

## Pending Items

* [ ] Enriched views
    * [ ] Candidate Enriched view (since Hoover++ does NOT store ad and slot information in the candidate view, we will provide an enriched view that has this information instead of bloating the data model)
* [ ] PR merges
    * [ ] <https://github.freewheel.tv/data/hoover-model/pull/424>
* [ ] Analysis (below tables need to be analyzed; for ack it’s re-analysis (after above PR merge) and for candidate we need 1st round of validations)
    * [ ] Ack
    * [ ] Candidate

## Known Differences

| **Table** | **Column Name(s)** | **Reason** | **Will it be changed?** |
| --- | --- | --- | --- |
| Ack | `avails_category__*`ALL `in_played_slot` metricsApplies to both `partner` and `eligible_outbound_order` fields. | Multipliers`request_magnifier * ack_multiplier * log_sampling_magnifier`All in played slot metrics have the multiplier applied to them. For instance, if the value of `unfilled_avails_in_played_slot` is 1 in Hoover, then the value in Hoover++ with the multipliers applied is 100. | NO |
| Ack | `avails_event_count` | This column is NOT available as a selectable column in Hoover++. Similar to above, the value of this is already applied to the avails metrics. So downstream just needs to `SELECT` the column they want without having to multiply. | NO |
| Ack | `cpx` events | In Hoover++, when we build the adMap, we use the ad\_id + ad\_rendition\_id combo. There are cases, especially for CPX, when these are not available on the request but are available on the callback. When the callback is tried to be linked to the request, it fails since the map doesn’t contain the combo. | YESFix will be worked on in a later release. |
| Ack | Differences between `ack__metrics__ad_impression` between Hoover \<\> Hoover++ | we don't have a corresponding metrics in H++ which makes more sense from metrics definition, that's the new design. If you really want to find something to align, then use traffic\_type != 1 +`ack__metrics__ad_impression` (in H++), it should be = `ack__metrics__ad_impression in current hoover` | @Li, Ruonan |
| Ack | Missing `network_execution_ctx_index` for `ad` impression callbacks | can not really remember why we need to have the **network\_execution\_ctx\_index **set for ad impression callbacks in H++. guessing we need to overwrite some values in the original network\_execution\_ctx based on the info carried in  ad impression callback.I can not think of a scenario where we will have to rely on this index on slot impression, because the index is already set on slot entity where this slot impression belongs to. so I would suggest we just treat this as an expected differences and document it somewhere in the wikiThe index on ad impression callback is properly set and the missing part is only for slot impression | NO |
| Ack | `inventory__asset_chain__reseller_network_id``inventory__asset_chain__supply_source``inventory__asset_chain__sales_channel``inventory__asset_chain__floor_price``inventory__site_section_chain__floor_price``ack__ad_unit_id``ack__metrics__ad_net_avail``ack__metrics__ad_gross_avail``ack__metrics__ad_unconstrained_gross_avail``advertisement__billable_rate_denominator_event_id``advertisement__provider_measured_event_id``advertisement__original_bidding_price``advertisement__matched_geo_ids``partners__inbound_listing_ids``candidate__trust_id``ads_in_slot__partners__internal_deal_ids` | All values are `NULL` [LQS Link Example](https://lqs.fwmrm.net/#datasource=prd-internal-presto&engine=presto&tab=result&queryid=presto_20260722021731_260302&externalid=20260722_021736_00046_ptdgd) | NO |
| Ack | `inventory__*` both `site_section_chain` and `asset_chain` | This is an enhanced data capability in Hoover++. Essentially Hoover does not report these fields in the `ack` table but Hoover++ does since it’s a shared entity. | NO |
| Ack | `partners__rule_ext_id` | All values are either `-1` or `null`No point of adding in[LQS link](https://lqs.fwmrm.net/#datasource=prd-internal-presto&engine=presto&tab=result&queryid=presto_20260722214408_584776&externalid=20260722_214411_00313_pw3w5) | NO |
| Ack | Duplicate callback rows | This is because the kafka broker is sometimes restarted and since the PRD testing environment does not have de-duplication, same keys and records make it through.This will NOT happen in the actual PRD environment. | NOT AN ISSUE IN PRD |
| Ack | `inventory__asset_chain__*` and `inventory__site_section_chain__*` | These fields are populated for the ack table as well in Hoover++. Since it’s a shared entity (1 copy of the entity for all tables), this is an enhanced data capability. | NO |
| Ack | `is_filtered` flag not set in code | [Entity - AckCtx](https://freewheel.atlassian.net/wiki/spaces/Infrastructure/pages/191808341/Entity+-+AckCtx)This is by design since the filtered flag can be updated by post-bid IVT. Downstream users should add a check:`ack__flags & 256 = 0`Example → [LQS Link](https://lqs.fwmrm.net/#datasource=prd-internal-presto&engine=presto&tab=result&queryid=presto_20260728191450_817346) | NO |
| Ack | Virtual columns`ack__metrics``partners``ads_in_slot__candidate``ads_in_slot__partners` | Virtual columns are mostly excluded from Hoover++. Namely `ack__metrics` , `partners`, `ads_in_slot__candidate` etc.  This is because Hoover++ acks are a culmination of 3 types of acks – request, ad and slot – and safely doing a UNION is not feasible | NO |
| Ack | `partners__bit_flags``partners__audience_segment_max_cpm``partners__audience_partner_segment_infos__audience_partner_id``partners__audience_partner_segment_infos__max_cpm``partners__audience_partner_segment_infos__matched_segments__id``partners__audience_partner_segment_infos__matched_segments__cpm``advertisement__active_aim_audience_ids``advertisement__effective_exclude_aim_audience_ids``partners__audience_partner_segment_infos__matched_segments__flags``ads_in_slot__partners__audience_partner_segment_infos``ads_in_slot__partners__audience_partner_segment_infos__matched_segments__id` | AIM model is not yet integrated into Hoover++ | YES |
| AckSlot | `partners__supply_priority``partnres__acquired_supply_type` | Being worked on by @Marino Johnson, Daniel | YESFix will be worked on. |
| Ad | `advertisement__bit_flags``advertisement__replaced_bit_flags``advertisement__effective_exclude_aim_audience_ids``partners__bit_flags``partners__audience_partner_segment_infos``partners__audience_segment_max_cpm` | This is because the AIM model has not been integrated into Hoover++. | YESFix will be worked on in a later release. |
| Ad | `partners__internal_deal_ids``partners__inbound_listing_ids` | Fields are `null` in the Ad table; no point adding them in.[LQS Link 1](https://lqs.fwmrm.net/#datasource=prd-internal-presto&engine=presto&tab=result&queryid=presto_20260729180247_220490&externalid=20260729_180313_00120_vzte4) and [LQS Link 2](https://lqs.fwmrm.net/#datasource=prd-internal-presto&engine=presto&tab=result&queryid=presto_20260729180325_552403&externalid=20260729_180355_00121_vzte4) | NO |
| AckAd | `partners__listing_id` | Needs further investigation as to why there’s a diff | YES |
| Auction | `auction__metadata_auditing_bit_flags` | Needs further debugging as to why it’s not set in Hoover++ | YES |
| Candidate | `slot_*``ad_*` | Generally speaking, there is no relationship between candidate \<=\> slot. A relationship can only be established after the ad selection process and there is a selected ad on that candidate. In this case, a corresponding slot can be defined by following the route candidate -\> selected ad -\> slot. But for other candidates without selected ad, we can not link them into any slots.Hence, the candidate context and candidate BCV both do NOT include the slot and ad entities. | NO |
| Request | `inventory__site_section_chain__tracked_audience_item_ids``audiences__network_id` | Hoover++ reports `no` values for this field while it’s fully populated in Hoover. This is a known diff but we have NOT had a chance to dive deeper. | YES |
| ALL | `partners__site_group_id` | Column is named differently in Hoover++. It is called `site_section_group_id`. Will be fixed | YES |
| ALL | `request__mrc_compliance_label``request__traffic_compliance__mrc_compliance_flag``request__is_filtered``request__traffic_type``request__backend_filtration_reason``request__client_facing_ivt_reason_flag``ack__traffic_type``ack__client_facing_ivt_reason_flag``visitor__filtration_reason` | These are different because of post bid IVT differences in Hoover \<\> Hoover++ | @Li, Ruonan |
| ALL | `request__identifier__source``ack__identifier__source` | These will NOT be added to Hoover++This is because you can get the same information from `kafka_msg_key`Additionally size (in TiB) is too high (\> 0.03 TiB) | NO |
| ALL | `request__hashed_key_value` | The value for this is different because Hoover++ calculates this from the stripped (GDPR/ CCPA/ Privacy logics applied) binlogThis field is mainly used by IVT; so this may be ok | @Li, Ruonan |
| ALL | `request__traffic_type``request__backend_filtration_reason``request__client_facing_ivt_reason_flag` | Due to postbid IVT changes, sometimes there are traffic\_type differences when comparing requests between Hoover \<\> Hoover++Traffic\_type indicates ivt status (0: valid, 1:invalid-marked in prebid, 2:invalid-marked in postbid). the possible IVT diff comes from postbid ivt, because it is using different data between hoover and hoover++. so it means there may be some ivt traffic(traffic\_type=2) in the current hoover being marked as valid(traffic\_type=0) in h++ or the other way round. This may lead to differences in the ack table/ view for different dimensions/ metrics | @Li, Ruonan |
| ALL | `request__bid_request__auction_type` | H++ is correct | NO |
| ALL | `request__flags` | This difference is due to the fact that flusher will always try to merge the callbacks within 5-min window to its first\_request if the first\_request also appears in the same 5-min window. This is an internal technical optimization to reduce the flush table size and will not have any impacts to reporting business.For traffic\_type = 2, request\_\_flags are also set by the IVT compaction pipeline.Similar to above, FILTERED flag is set by postbid IVT. | IVT part → YESNon IVT part → NO(aka PRIMARY\_REQUEST) |
| ALL | `request__bit_flags` | Sometimes the is\_forecast\_exclude BIT FLAG is not set. Since this is only used by forecasting (AF) team; we will get to the bottom of this after MVP | YES |
| ALL | MISSING FIELDS  (`request__scores` for example) | Since some columns are not present in Hoover++ (they’re not used by downstream) they were removed. Hence the diff when checking the full column. | NO |
| ALL | `inventory__asset_chain__geo_visibility__report_aggregate``geo_visibility_*``ads_in_slot__partners__geo_visibility__report_aggregate` | geo\_visibility is deprecated hence not implemented in Hoover++ | NO |
| ALL | `request__ifa_type` and `request__ifa` | Requires a FULL copy of the raw KVs. For cost and compute savings, this is not added to Hoover++ | NO |
| ALL | `request__context__extra_key_value``request__context__extracted_key_value___fw_dbp``request__context__extracted_key_value___fw_lto` | We store the FULL stripped KV in the request. That can be used to get the value of these 2 keys | NO |

| **Column Name** | **Known** | **Comment** |
| --- | --- | --- |
| request\_\_flags | Y | This difference is due to the fact that flusher will always try to merge the callbacks within 5-min window to its first\_request if the first\_request also appears in the same 5-min window. This is an internal technical optimization to reduce the flush table size and will not have any impacts to reporting business.For traffic\_type = 2, request\_\_flags are also set by the IVT compaction pipeline. |
| request\_\_is\_filtered | Y | This is most likely because the IVT compaction pipeline isn’t running yet. The IVT entity also sets the value of request\_\_flags POST IVT. |
| MISSING FIELDS  (request\_\_scores for example) | Y | Since some columns are not present in Hoover++ (they’re not used by downstream) they were removed. Hence the diff when checking the full column. |
| \*\_in\_played\_slot metrics in Slot | Y | All \*\_in\_played\_slot metrics are `null` because they’re never populated in the Slot table (they come from the ack table. So seeing an array of null vs the whole value being null, is semantically correct. |
| eligible\_outbound\_orders (schema difference) | Y | Eligible outbound orders schema is different in Hoover \<\> Hoover++. Since not all columns are available, this is expected. |
| eligible\_outbound\_orders\_\_order\_priority | Y | If an order is truly `null`, instead of an empty arraylist, it returns `null`. Semantically the same. |
| eligible\_outbound\_orders\_\_order\_transaction\_type | Y | same as above |
| eligible\_outbound\_orders\_\_order\_type | Y | same as above |
| partners\_\_inbound\_listing\_ids (slot view) | Y | This was renamed to `inbound_listing_id` to avert confusion. Since there’s no `inbound_listing_ids` in Slot partners in Hoover (only in Auction Partners), this was changed. |
| Missing `network_execution_ctx_index` for `ad` impression callbacks | Y | From @Wang, Yucan not really remember why we need to have the **network\_execution\_ctx\_index **set for ad impression callbacks in H++. guessing we need to overwrite some values in the original network\_execution\_ctx based on the info carried in  ad impression callback.I can not think of a scenario where we will have to rely on this index on slot impression, because the index is already set on slot entity where this slot impression belongs to. so I would suggest we just treat this as an expected differences and document it somewhere in the wiki |
| Duplicate rows (especially for acks) | Y | This is because the kafka broker is sometimes restarted and since the PRD testing environment does not have de-duplication, same keys and records make it through.This will NOT happen in the actual PRD environment. |
| `outbound_order` vs `eligible_outbound_order` | Y | From @Wang, Yu :the eligible\_outbound\_orders is the enriched version, we need someone to dive into this logics and understand the difference from the raw field. adding the raw back is easy, but I am still reluctant to do so until we are 100% sure the raw is still neededThere is a ticket out there to track this work. Once we know what the differences are and why, we can decide if we need to add this field back into Hoover++ |
| Differences between ack\_\_metrics\_\_ad\_impression between Hoover \<\> Hoover++ | Y | we don't have a corresponding metrics in H++ which makes more sense from metrics definition, that's the new design. If you really want to find something to align, then use traffic\_type != 1 +`ack__metrics__ad_impression` (in H++), it should be = `ack__metrics__ad_impression in current hoover` |
| inbound\_order\_transaction\_type | N | For resellers, inbound\_order\_transaction\_type is NOT set. PR → <https://github.freewheel.tv/data/hoover-model/pull/371> |
| partners\_\_bit\_flags | N | Hoover++ maps `BIT_FLAG_NETWORK_IS_CAMPAIGN_MANAGER_DEAL_BUYER` to `CARRIAGE_ORDER` instead of `CAMPAIGN_MANAGER_ORDER`. This  explains the `2^37` delta between Hoover and Hoover++: in legacy Hoover (`setSlotPartnerBitFlags`), bit 37 is set only for `CAMPAIGN_MANAGER_ORDER`, while in Hoover++ (`setNetworkBitFlags`) it is set for `CARRIAGE_ORDER`.  PR submitted: <https://github.freewheel.tv/data/hoover-model/pull/385> |
| inventory\_\_asset\_chain\* inventory\_\_site\_section\_chain\* in ack table | Y | In the old Hoover table these are null most of the time: <https://github.freewheel.tv/data/etl/blob/master/etl-schema-hoover/src/main/java/tv/freewheel/hoover/table/AckTable.java#L77> (only set for the video case)We will leave it because this is more data capabilityThread here: <https://freewheel.slack.com/archives/C01S31V42LX/p1782841339175119> |

## Detailed Analysis

[https://freewheel.atlassian.net/wiki/spaces/Infrastructure/pages/790233532/Field-level+analysis+on+BCV+Rough+Work+Plan?atl\_f=PAGETREE](https://freewheel.atlassian.net/wiki/spaces/Infrastructure/pages/790233532/Field-level+analysis+on+BCV+Rough+Work+Plan?atl_f=PAGETREE)
