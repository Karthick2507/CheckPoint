# ETL + data-quality framework dependencies
trino[sqlalchemy]     # Presto/Trino connector (SQLAlchemy dialect)
typer                 # CLI
great_expectations    # validation engine
pandas                # GE / tabular support
pyyaml                # config loading
jinja2                # SQL templating ({{ batch_id }} in pipeline configs)
rich                  # console reporting
